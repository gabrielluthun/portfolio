---
import { ClientRouter } from "astro:transitions";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import SmoothScroll from "../components/SmoothScroll.tsx";
import BackToTop from "../components/BackToTop.tsx";
import { sitePath } from "../lib/site-path";
import "../styles/global.css";

type Props = {
  title?: string;
  description?: string;
};

const {
  title = "Gabriel Luthun — Portfolio",
  description = "Développeur full-stack. Je transforme vos besoins métier en produits simples et utiles.",
} = Astro.props;
const favicon = sitePath("favicon.svg", import.meta.env.BASE_URL);
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href={favicon} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <title>{title}</title>
    <script is:inline>
      const themeKey = "theme";
      const applyStoredTheme = () => {
        document.documentElement.classList.toggle(
          "dark",
          localStorage.getItem(themeKey) === "dark",
        );
      };
      applyStoredTheme();
      document.addEventListener("astro:after-swap", applyStoredTheme);
    </script>
    <ClientRouter />
  </head>
  <body class="flex min-h-screen flex-col bg-bg text-fg">
    <a
      href="#contenu"
      class="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-50 focus:rounded-md focus:bg-accent focus:px-3 focus:py-2 focus:text-accent-fg"
    >
      Aller au contenu
    </a>
    <SmoothScroll client:load />
    <BackToTop client:load />
    <Header />
    <main id="contenu" class="flex-1" transition:animate="fade">
      <slot />
    </main>
    <Footer />
  </body>
</html>
